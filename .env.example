# Next.js Portfolio - Environment Variables
# Copy this file to .env.local and fill in your values

# =============================================================================
# CHAT BACKEND (REQUIRED FOR /api/chat and /api/chat/eval)
# Production chat is gpt-oss hosted on a local MacBook Pro M4 Max via
# an OpenAI-compatible endpoint.
# =============================================================================
INFERENCIA_BASE_URL=http://localhost:8080/v1
INFERENCIA_API_KEY=replace-with-your-openai-compatible-key
INFERENCIA_CHAT_MODEL=mlx-community/gpt-oss-20b-MXFP4-Q8

# Optional: protects /api/chat/eval when testing against a remote environment.
# If unset, eval endpoint only allows local requests.
# CHAT_EVAL_TOKEN=replace-with-random-secret

# =============================================================================
# RATE LIMITING
# =============================================================================
CHAT_MAX_RPM_PER_IP=2
CHAT_MAX_MESSAGES_PER_SESSION=10
NEXT_PUBLIC_CHAT_MAX_MESSAGES=10
CHAT_DAILY_BUDGET=150

# =============================================================================
# ANALYTICS — Google Analytics 4 (optional; no Vercel analytics)
# =============================================================================
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# =============================================================================
# SITE + GCP TELEMETRY (OPTIONAL)
# =============================================================================
NEXT_PUBLIC_SITE_URL=https://gimenez.dev
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id
# GOOGLE_CLOUD_REGION=us-east1
